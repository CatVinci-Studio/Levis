//! The bits of app identity that get threaded into the (otherwise
//! app-agnostic) `aicompat` crate: what to call this app in OAuth
//! `originator` params and in the post-login browser page.

pub const APP_NAME: &str = "Levis";
pub const ORIGINATOR: &str = "levis";
